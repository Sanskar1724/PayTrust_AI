# PayTrust AI — Local Streamlit Prototype (Phase 1)
# Python 3.11 / 3.12 — keep it lightweight, no Docker/Postgres required

streamlit>=1.55,<2
python-dotenv==1.0.1
pydantic>=2.7.4
pydantic-settings>=2.3.3

# Database — SQLite (stdlib) + aiosqlite for async where needed
aiosqlite==0.20.0

# Data / ML (local, no GPU) — use wheels, avoid meson build on Windows without VS Build Tools
pandas>=2.2.0
numpy>=1.26.4
scikit-learn>=1.5.0
plotly>=6.0
scipy>=1.13.0
joblib>=1.4.2
pyarrow>=18.0.0   # parquet read/write (IEEE test predictions, train chunks, submission)

# HTTP clients for AI providers + Razorpay
httpx==0.27.0
requests==2.32.3
# LLM SDKs are NOT required: engines/ai_engine.py calls OpenRouter / Groq /
# Gemini over plain HTTP via httpx (no openai / google-generativeai import
# anywhere). Commented out to fix protobuf<5 vs Streamlit>=5.26 conflict.
# openai==1.35.10
# google-generativeai==0.8.6

# FastAPI HTTP service layer (Phase A — exposes tested engines as a REST service)
fastapi>=0.115
starlette>=0.37  # explicit: api/security.py imports BaseHTTPMiddleware from starlette
uvicorn[standard]>=0.30

# Auth / Security (test-mode only)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Logging / Utils
structlog>=24.1.0
python-json-logger>=2.0.7
pyyaml>=6.0.3
email-validator>=2.1.1

# Testing
pytest>=8.2.2
pytest-asyncio>=0.23.3
pytest-cov>=5.0.0
