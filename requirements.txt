# PayTrust AI — Local Streamlit Prototype (Phase 1)
# Python 3.11 / 3.12 — keep it lightweight, no Docker/Postgres required

streamlit==1.39.0
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.3

# Database — SQLite (stdlib) + aiosqlite for async where needed
aiosqlite==0.20.0

# Data / ML (local, no GPU)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
scipy==1.13.1
joblib==1.4.2

# HTTP clients for AI providers + Razorpay
httpx==0.27.0
requests==2.32.3
openai==1.35.10
google-generativeai==0.7.2

# Auth / Security (test-mode only)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Logging / Utils
structlog==24.1.0
python-json-logger==2.0.7
pyyaml==6.0.1
email-validator==2.1.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.3
pytest-cov==5.0.0
